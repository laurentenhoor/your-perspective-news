import angular from 'angular';

import DebateTileComponent from './debate-tile.component';

export default angular
    .module('DebateTile', [])
    .component('debateTile', DebateTileComponent)
    .name;