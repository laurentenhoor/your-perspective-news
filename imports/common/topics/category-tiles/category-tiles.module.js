import angular from 'angular';

import CategoryTilesComponent from './category-tiles.component';

export default angular
    .module('CategoryTiles', [])
    .component('categoryTiles', CategoryTilesComponent)
    .name;