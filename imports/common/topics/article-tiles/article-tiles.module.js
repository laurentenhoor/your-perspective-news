import angular from 'angular';

import ArticleTilesComponent from './article-tiles.component';

export default angular
    .module('ArticleTiles', [])
    .component('articleTiles', ArticleTilesComponent)
    .name;