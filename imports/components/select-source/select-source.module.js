import angular from 'angular';

import SelectSourceComponent from './select-source.component';

export default angular
    .module('SelectSource', [])
    .component('selectSource', SelectSourceComponent)
    .name;